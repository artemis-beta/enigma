version: 2
jobs:
    build:
       docker:
               - image: python:latest
       steps: 
         - checkout
         - run:
              name: Create virtualenv
              command: python -m venv myenv
         - run:
              name: Install app and test
              command: |
                      . myenv/bin/activate
                      pip install .
                      nose2
